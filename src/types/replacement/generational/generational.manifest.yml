id: replacement/generational

properties:
  elitism:
    type: Integer
    description: >
      The number of elite individuals within a deme which should be retained
      at the end of a generation.

composer: |
  GenerationalReplacement()
