id: fitness/moga
description: |
  Multiple Objective Genetic Algorithm.
dependencies:
  - fitness/pareto
files:
  - moga.jl
composer: |
  s["of"] = Base.eval(Base.parse(Base.get(s, "of", "Float")))
  MOGAFitnessScheme{s["of"]}(s["maximise"])
